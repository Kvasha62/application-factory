# ADR-0010 — Аудит архитектурных пробелов: ARCHITECTURE.md 1.1.0 → 1.2.0

**Статус:** `RATIFIED` — принято владельцем проекта 2026-09-08
**Дата:** 2026-09-08
**Уровень:** **Level D — изменение конституции**
**Инициатор:** архитектурный аудит ARCHITECTURE.md 1.1.0
**Ратификация:** явное подтверждение владельца проекта в рабочей сессии 2026-09-08

## 1. Контекст

ARCHITECTURE.md 1.1.0 является техническим законом проекта. Аудит проверил три слоя: сам закон, существование документов, на которые он ссылается, и фактическое состояние репозитория.

На момент аудита репозиторий содержал только основу: `.gitignore`, README, `pyproject.toml`, ARCHITECTURE.md и IDE-файлы. Кода, компонентов, манифестов, Golden Bundle, Platform Instances, схем, миграций, контрактных тестов и CI не было.

В ходе аудита подтверждено, что в текущем репозитории и его истории отсутствуют `ADR-0009`, `docs/07-core-model.md`, `docs/08-scenario-validation.md`, `docs/01-architecture.md`, `docs/04-tech-stack.md`, `docs/adr.md` и `RELEASE_POLICY.md`.

**Исторический факт:** ADR-0009 не восстанавливается предположением. Его отсутствие является установленным пробелом происхождения редакции 1.1.0.

Положительный вывод: компонентов и поставленных инстансов нет, поэтому конституционные уточнения сейчас не требуют миграции существующих систем. Это последнее безопасное окно до публикации первого компонента.

## 2. Проблемы

### P1 — Закон требует неформализованных артефактов

Component Contract, Golden Bundle, Platform Manifest, Tenant, Saga и модель идемпотентности упоминаются законом, но не имеют достаточного машиночитаемого определения, идентичности и жизненного цикла.

### P2 — Закон не имеет механизма проверки

В конституции есть проверяемые правила, но отсутствует общий принцип доказательства соответствия. В репозитории нет CI, схем и валидаторов. Это operational gap, а не основание выбирать конкретный технологический стек.

### P3 — LAW-16 и §24 требуют классификации областей данных

Запрет доступа к чужому tenant не определяет статус данных самой платформы, каталога, реестров и агрегированной аналитики. Без классификации `tenant-scoped` / `platform-scoped` / `system-scoped` закон противоречив.

### P4 — Ратификация 1.1.0 не подтверждена существующими артефактами

ARCHITECTURE.md ссылается на отсутствующие ADR-0009 и документы в доказательствах валидации. Нельзя считать ссылку на несуществующий документ достаточным доказательством.

### P5 — Пустая кодовая база не является нарушением

§4.1 разрешает standalone-режим до достижения фабричных гейтов. Поэтому отсутствие Component Catalog, Golden Bundles, Composer и Release Train сейчас является допустимым состоянием.

## 3. Полный реестр F-001…F-034

Ниже находится **канонический реестр данного аудита**. Отдельный файл `docs/architecture-review-1.1.0.md` был поглощён настоящим ADR и удалён.

| ID | Класс | Серьёзность | Суть | Решение |
|---|---|---|---|---|
| F-001 | F5 | Высокая | Нет ADR-0009 и каталога ADR; ратификация 1.1.0 без записи | M2; AMD-12 |
| F-002 | F5 | Высокая | Нет `docs/07-core-model.md`, на который ссылается закон | M2 |
| F-003 | F5 | Высокая | Нет `docs/08-scenario-validation.md`, хотя §40 утверждает выполненную валидацию | M2 |
| F-004 | F5 | Средняя | Нет `docs/01-architecture.md` и `docs/04-tech-stack.md` | M2: убрать фиктивные нормативные ссылки |
| F-005 | F5 | Средняя | Нет `docs/adr.md` для исторических ADR-0001…0008 | M2 |
| F-006 | F5 | Низкая | Нет `RELEASE_POLICY.md` | Создать при фабричном режиме либо убрать ссылку до его наступления |
| F-007 | F1 | Низкая | README недостаточно явно связывает проект с архитектурным законом | Исправлено в README |
| F-008 | F2 | Высокая | Component Contract не формализован как артефакт | AMD-01 |
| F-009 | F2 | Средняя | UI integration protocol отсутствует в контракте | AMD-01 |
| F-010 | F2 | Средняя | Data Export / CDC не имеет формальной версии в контракте | AMD-01 |
| F-011 | F2 | Высокая | Tenant отсутствует в иерархии модели | AMD-02 |
| F-012 | F2 | Высокая | Не определены создание, владение и источник истины Tenant | AMD-02 |
| F-013 | F2 | Высокая | Нет lifecycle Tenant и правил распространения контекста | AMD-02 |
| F-014 | F4 | Высокая | LAW-16 конфликтует с кросс-tenant аналитикой §24 | AMD-02 + LAW-16a |
| F-015 | F2 | Критическая | Можно формально передать подменённый tenant_id от вызывающей стороны | AMD-03 |
| F-016 | F2 | Высокая | Не определено удаление Tenant | AMD-02 + AMD-11 |
| F-017 | F1 | Средняя | Неоднозначно используется термин «клиент» | Уточнить при редакции 1.2.0 |
| F-018 | F2 | Высокая | Saga Contract не определён | AMD-04 |
| F-019 | F2 | Высокая | Идемпотентность определена только для событий | AMD-05 |
| F-020 | F2 | Высокая | Нет модели межсервисной аутентификации | AMD-06 |
| F-021 | F2 | Высокая | Нет единой модели авторизации | AMD-06 |
| F-022 | F2 | Высокая | Нет единой модели идентификаторов | AMD-07 |
| F-023 | F2 | Высокая | Нет lifecycle Platform Manifest | AMD-08 |
| F-024 | F2 | Средняя | Manifest не имеет достаточных id/digest/signature/predecessor | AMD-07 + AMD-08 |
| F-025 | F2 | Высокая | Не определено, что означает certified Golden Bundle | AMD-09 |
| F-026 | F2 | Высокая | Golden Bundle не имеет иммутабельной идентичности и digest | AMD-09 |
| F-027 | F2 | Средняя | Не определён отзыв Golden Bundle | AMD-09 |
| F-028 | F2 | Средняя | Нет единого контекста телеметрии и правил распространения | AMD-10 |
| F-029 | F2 | Высокая | Не определён lifecycle данных | AMD-11 |
| F-030 | F2 | Высокая | Нет обязательной модели аудита | AMD-06 + AMD-11 |
| F-031 | F5 | Высокая | Нет механизма проверки исполнения архитектурного закона | AMD-12 |
| F-032 | F5 | Средняя | Нет инфраструктуры схем, валидаторов, DoD-механизма и минимальной проверки | Реализация уровня A после ратификации |
| F-033 | F5 | Низкая | Нет машиночитаемого реестра владельцев компонентов | CODEOWNERS + owner в контракте |
| F-034 | F0 | Информационная | Кодовая база пуста; нарушений реализации нет | Ничего |

**Итог:** F0 — 1; F1 — 3; F2 — 20; F3 — 0; F4 — 1; F5 — 9. Всего 34 замечания.

## 4. Решение

Принять подготовку **ARCHITECTURE.md 1.2.0** как минимальное аддитивное расширение 1.1.0. Ни один существующий LAW-01…LAW-16 не отменяется. Конкретные технологии не выбираются этим ADR.

### AMD-01 — Дескриптор контракта компонента

Каждая версия компонента публикует машиночитаемый дескриптор публичного контракта. Он содержит минимум: `api`, `events`, `data_export_cdc`, `ui`, `configuration_schema`, `data_ownership`, `authn`, `authz`, `compatibility_policy`, `dependencies`. Необъявленная поверхность является внутренней и не защищается контрактом совместимости.

### AMD-02 — Tenant и области данных

Tenant становится сущностью уровня Platform Instance. Tenant Authority владеет реестром и lifecycle: `provisioning → active → suspended → deletion_requested → deleted`.

Каждый набор данных относится ровно к одной области:
- `tenant-scoped` — данные арендатора, по умолчанию все бизнес-данные;
- `platform-scoped` — данные Platform Instance;
- `system-scoped` — данные фабрики.

Кросс-tenant операции разрешены только над `platform-scoped` и `system-scoped` данными и должны быть авторизованы и отражены в аудите. Удаление Tenant является контрактно видимым lifecycle-событием.

### LAW-16a — Data Scope Classification

Любой набор данных обязан иметь ровно одну область. Область объявляется в Component Contract. Кросс-tenant обработка `tenant-scoped` данных запрещена.

### AMD-03 — Целостность tenant-контекста

Tenant-контекст выводится из проверенной идентичности, а не принимается как доверенный входной параметр. Переданный вызывающей стороной `tenant_id` используется только для cross-check и отклоняется при несовпадении. Исключение — явно авторизованные `platform-scoped` операции.

### AMD-04 — Saga Contract

Минимальный контракт саги: `saga_id`, `correlation_id`, `tenant_id` либо явная область, `step_id`, `state`, `retry_policy`, `timeout`, `compensation`, `idempotency`, `terminal_state`.

Компенсация объявляется до выполнения шага и сама идемпотентна. Терминальные состояния: `completed`, `compensated`, `failed`. Таймаут не является молчаливым успехом.

### AMD-05 — Идемпотентность

Определяются три класса:
1. события — `event_id`, дедупликация не менее окна повторной доставки;
2. API — `Idempotency-Key` для неидемпотентных по природе операций изменения состояния;
3. команды — `command_id`, включая шаги саги.

Повтор с тем же ключом не создаёт второй эффект. Replay является отдельной операцией и не смешивается с retry.

### AMD-06 — Идентичность и безопасность

Человек → OIDC. Сервис, фоновая задача и агент → собственная проверяемая service identity. Авторизация применяется на границе компонента-владельца данных. Аутентификация вызывающей стороны не заменяет авторизацию. Изменяющие состояние и tenant-scoped чтения подлежат аудиту.

### AMD-07 — Модель идентификаторов

Разделяются identity и version для `platform_id`, `component_id`, `component_version_id`, `manifest_id`, `bundle_id`, `tenant_id`, `event_id`, `saga_id`, `request_id`, `correlation_id`, `contract_id`.

Идентификатор иммутабелен, непрозрачен, уникален в своей области, не переиспользуется и не кодирует бизнес-смысл. Для поставляемых артефактов обязательны `id + version + digest`. Конкретный формат UUIDv7/ULID и т.п. остаётся временным решением.

### AMD-08 — Lifecycle Manifest

Полный lifecycle: `draft → validated → approved → published → deployed → superseded → retired`. Для standalone допускается упрощённый профиль с одной записанной аттестацией валидации.

`published` иммутабелен и содержит `manifest_id + version + digest + predecessor`. `approved` содержит сведения об утверждении. Нельзя переписывать опубликованный манифест под тем же identity/version.

### AMD-09 — Golden Bundle

Golden Bundle — сертифицированный, воспроизводимый набор совместимых компонентов и версий. Lifecycle: `draft → candidate → certified → deprecated → revoked`.

`certified` означает прохождение утверждённого набора проверок совместимости и безопасности. Поставляемый bundle иммутабелен и идентифицируется `bundle_id + version + digest`; evidence привязывается к digest. `revoked` запрещает новые поставки, но не означает автоматическое разрушение уже работающих инстансов.

### AMD-10 — Observability Context

Обязательный контекст телеметрии, где применимо: `timestamp`, `environment`, `platform_id`, `component_id`, `component_version`, `tenant_id`, `request_id`, `trace_id`, `correlation_id`, `saga_id`, `actor/service_id`.

Контекст не должен позволять пересечь tenant boundary; чувствительные значения не передаются без необходимости.

### AMD-11 — Data Lifecycle и Audit

Для каждого класса данных определяется lifecycle: `created → active → archived/retained → deleted`, с политикой retention. Удаление tenant запускает согласованный процесс удаления/анонимизации tenant-scoped данных. Аудит является отдельным неизменяемым журналом событий безопасности и изменения состояния; срок хранения задаётся политикой данных.

### AMD-12 — Conformance

Каждое машиночитаемое правило закона должно иметь automated check, contract test или явно зарегистрированную manual review procedure. Level C/D изменения требуют ADR. Любая новая RATIFIED редакция архитектуры должна иметь реальный ADR с подтверждённой историей.

## 5. План миграции

### M0 — Ратификация

ADR-0010 ратифицирован владельцем проекта 2026-09-08.

### M1 — Историческая очистка

Отдельный аудит-файл поглощён настоящим ADR и удалён. ADR-0009 не создаётся задним числом.

### M2 — Конституционная очистка

В ARCHITECTURE.md удалить фиктивные нормативные ссылки и утверждения, основанные на отсутствующих артефактах; сохранить только подтверждённую историю и действующие правила.

### M3 — Выпуск ARCHITECTURE.md 1.2.0

Интегрировать AMD-01…AMD-12 и LAW-16a, обновить §0.2, §4.1, §5.4, §6.2, §24, §34, §40 и §41 по фактической истории.

### M4 — Documentation Graph Audit

Проверить ссылки из нормативных документов на существующие файлы и исторические записи. Ссылки на отсутствующие артефакты не считаются доказательством.

### M5 — Documentation Baseline

После успешного M4 зафиксировать состояние документации как CLEAN для текущего standalone-режима.

### M6 — Первый implementation slice

Только после CLEAN baseline выбрать и реализовать минимальный технический срез, необходимый для первого компонента. Выбор конкретного стека оформляется отдельным ADR/документом соответствующего уровня.

## 6. Политика документации

- `docs/ARCHITECTURE.md` — единственный действующий архитектурный закон.
- `docs/adr/` — единственный каталог ADR и архитектурной истории.
- Один предмет → один канонический документ.
- Запрещены конкурирующие `*-final`, `*-new`, `*-v2` документы.
- Отсутствующий исторический документ не реконструируется предположением.
- Документ не может считаться доказательством только потому, что на него ссылается другой документ.

## 7. Матрица покрытия

| Проблема | ADR-0010 | ARCHITECTURE 1.2.0 | Implementation |
|---|---|---|---|
| Provenance gap | Да | Да | — |
| Component Contract | AMD-01 | Да | Да |
| Tenant/data scopes | AMD-02/03 | Да | Да |
| LAW-16 contradiction | LAW-16a | Да | Да |
| Saga/idempotency | AMD-04/05 | Да | Да |
| Identity/authz | AMD-06/07 | Да | Да |
| Manifest | AMD-08 | Да | Да |
| Golden Bundle | AMD-09 | Да | Да |
| Observability | AMD-10 | Да | Да |
| Data lifecycle/audit | AMD-11 | Да | Да |
| Conformance | AMD-12 | Да | Да |
| Empty repository | P5/F-034 | Да | Не требуется |

## 8. Итоговый статус

**ADR-0010 RATIFIED.**

Это решение разрешает переход к ARCHITECTURE.md 1.2.0 и выполнение M2–M5. Оно не разрешает автоматически выбирать конкретный стек, начинать произвольную реализацию или сливать рабочую ветку в `main`.
